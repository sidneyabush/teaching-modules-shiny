# Data Folder

This folder contains data files downloaded from Google Drive.

## Downloading Data

To download the data files, run:

```r
source("../data_download.R")
```

## Note

Data files are excluded from version control via `.gitignore`. Each user must download the data independently using the `data_download.R` script.
